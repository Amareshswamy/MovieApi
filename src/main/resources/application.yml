spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/MovieApiDB

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format-sql: true

  servlet:
    multipart:
      max-file-size : 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: "swamyamaresh456@gmail.com"
    password: "dzna nokk zyxy pydf"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

project:
  poster: poster/

base:
  url: "http://localhost:8080"